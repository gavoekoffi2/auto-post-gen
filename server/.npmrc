# Fail the install on an unsupported Node rather than at test time.
#
# The API targets Node 22 (see "engines"), and its test files are TypeScript
# run with --experimental-strip-types. Without this, npm only warns, the
# install succeeds, and the failure surfaces later as something that looks
# unrelated to the runtime version.
engine-strict=true
